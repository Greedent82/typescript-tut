//for this exercise, try filling in the blanks in the type person object and in the createperson function!


export type Person = {
    //fill in this blank!
    //fill me in too!
  };
  
  export function createPerson(ADD TYPES HERE): Person {
    let person: Person = { name };
    if (age !== undefined) {
      person.age = age;
    }
    return person;
  }
  
